---
# INTERNAL TEMPLATE — reference only, never published.
# Excluded from getProjectSlugs()/getProjectBySlug() by the leading
# underscore in this filename, and from /work/[slug] static generation
# because that route sets `dynamicParams = false`.
# To add a real project: copy this file, rename it to the project's slug
# (e.g. example-project.mdx), and fill in the required fields below.
# Required fields already have clean placeholder values, so a straight
# copy-rename passes validation as-is (try `pnpm run validate:content`) —
# edit them next. Optional fields are commented out on purpose: uncomment
# and fill in only the ones you actually need, so nothing ships with
# leftover placeholder text pretending to be real data.
title: Example Project Name
type: E-commerce # one of: E-commerce | Product Design | Marketing Website | Design System
capabilities: # one or more: Ecommerce Systems | Brand & Identity | Custom Websites | Product Experiments; first item is the primary /work grouping
  - Ecommerce Systems
roles:
  - UI/UX Design
  - Front-End Implementation
services:
  - Service name shown alongside the project
summary: One or two sentences describing the project at a glance.
challenge: One or two sentences on the problem this project solved.
outcome: One or two sentences on the result, described qualitatively unless real metrics exist.
year: "2026"
featured: false
order: 100
status: draft # omit or set to "published" to make it live; "draft" hides it from getProjects()
# segment: D2C # one of: D2C | B2B — shown as a small chip next to `type` on cards; omit if not applicable
# client: Client name
# industry: Industry / sector label
# duration: "6 weeks"
# team: "Solo" or "2 designers, 1 developer"
# updatedAt (YYYY-MM-DD): the date this case study's content was last
# meaningfully revised. Powers the sitemap's lastModified for this project.
# Omit until the first real edit — don't set it to today's date just to
# fill it in, and don't touch it for typo fixes.
# updatedAt: "2026-01-15"
# coverImage/coverAlt must be set together — omit both until a real image
# exists rather than filling in placeholder text:
# coverImage: '/images/case-studies/example-cover.jpg'
# coverAlt: Descriptive alt text for the cover image
# seoTitle: SEO-specific title override — falls back to title if omitted
# seoDescription: SEO-specific description override — falls back to summary if omitted
# liveUrl: https://example.com
# repositoryUrl: https://github.com/example/example
---

<!-- No top-level "# " heading here — the page already renders an <h1> from
     frontmatter.title. Starting the body with "## " keeps the heading
     hierarchy correct (h1 -> h2), so copy this file as-is. -->

## Context
Write 3–5 lines about the client, the product, constraints, and your role.

## Problem
- What was broken?
- What was confusing or inefficient?
- What was the business or user impact?

## Strategy
Explain the key decisions — hierarchy, structure, trust, mobile, etc.

## Implementation
Describe the front-end/technical execution: stack, sections, performance, accessibility.

## Outcome
Describe the result qualitatively. Only include metrics if they are real and verifiable.
